# 复制为服务器上的 /opt/nextchat/.env

# 网页访问密码（逗号可多个）
CODE=nextchat-mio

# OpenAI 兼容上游（CPA 默认机内 8317，不要加 /v1）
BASE_URL=http://host.docker.internal:8317

# 可选：服务端默认 Key。留空则在网页设置里自己填（方便换站测）
OPENAI_API_KEY=

# 设为 1 则禁止页面自填 Key
HIDE_USER_API_KEY=
